---
---
callback([
    {% for image in site.static_files %}
        {% if image.path contains 'headshots' %}
            {
                "name": "{{image.name | replace:'"','\"'}}",
                "value": "{{ image.path }}"
            } {% unless forloop.last %},{% endunless%}
        {% endif %}
    {% endfor %}
])
